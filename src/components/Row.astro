---
interface Props {
	label: string;	
}

const {label} = Astro.props;
---

<div class="flex flex-col gap-1 md:flex-row md:gap-4">
	<div class="semi md:w-24 md:text-right">{label}</div>
	<div><slot /></div>
</div>
